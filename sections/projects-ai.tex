\section{Selected AI Projects}

\resumeRole{Moment}{In Development}{AI-Powered Planning App}{Swift, SwiftUI, TypeScript}
\begin{itemize}
  \resumeBullet{Building a native iOS app that turns requests such as planning a birthday into editable preparation plans.}
  \resumeBullet{Built a private prototype that saves the original request locally and shows AI output for review. Users can still save their input when AI is unavailable.}
  \resumeBullet{Developing schema validation and scenario-based checks for ambiguous requests, user confirmation, retries, and recovery before release.}
\end{itemize}

\resumeRole{LINE-Based Family Translator}{Private Pilot}{Chinese-Indonesian Translation}{Cloudflare Workers, D1, Local LLM}
\begin{itemize}
  \resumeBullet{Built a LINE bot for Chinese-Indonesian family communication; tested both directions in live private messages and confirmed the first family-group translation.}
  \resumeBullet{Connected Cloudflare Workers and D1 to a local LLM gateway; added webhook signature checks, message deduplication, and daily request limits.}
  \resumeBullet{Used 100 development cases to refine prompts, then evaluated a separate 40-case holdout set. Native-speaker review and always-on operation are still pending.}
\end{itemize}

\resumeRole{Development Task \& Handoff Tools}{Personal Tooling}{Local Sandbox}{CLI, HTTP, MCP, macOS}
\begin{itemize}
  \resumeBullet{Built CLI, HTTP, and MCP access to a shared local ticket store, with create, edit, recoverable delete, and restore operations.}
  \resumeBullet{Added revision and digest checks to reject stale edits, idempotency keys to prevent duplicate writes, and an audit history for recovering earlier ticket content.}
  \resumeBullet{Created tools to carry task context between agent sessions and inspect run status. Ticket editing does not start or control production workers.}
\end{itemize}
